import { IConfigProject } from "@/types";

export const CONFIG_PROJECT: IConfigProject = {
    name: "pokemon-poc",
    environment: "dev",
    description: "",
    version: "0.0.1",
    author: "Author",
}
